overwrite: true
schema: "schema.graphql"
generates:
  ./generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
